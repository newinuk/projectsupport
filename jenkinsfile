pipeline {
    agent any

    environment {
		dockerhub=credentials('dockerhub')
	}    
    stages {
         stage('clone') {
            steps {
                git branch: 'main', url: 'https://github.com/sankardevvinu/projectsupport.git'
            }
         }
        
         stage('Build') {
	      steps {
		  sh "docker build -t sankardev/task:tagname ."
	    }
	 }
         stage('Push') {
		  steps {
                sh 'docker push sankardev/task:tagname'
			}
		
		}
 }
} 	 
	 
